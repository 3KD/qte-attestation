# IBM backend + runtime instance
backend      = "ibm_kyoto"           # large IBM device in Japan
shots        = 400                    # hard cap per your cert budget
instance     = "ibm-q/open/main"     # change if you use a different hub/group/project
use_runtime  = true                   # try Qiskit Runtime first, else provider, else Aer

# Challenge knobs (demo)
n_qubits     = 8
key_seed     = 424242                 # trapdoor seed for keyed circuit

acceptor     = "allzeros"
